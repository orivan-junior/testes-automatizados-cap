{
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "allure-commandline": "^2.33.0",
    "allure-js-commons": "^3.2.1",
    "allure-playwright": "^3.2.1",
    "playwright-bdd": "^8.3.0"
  },
  "scripts": {
    "run_server_allure": "npx allure serve",
    "gerar-pasta-relatorio": "npx allure generate ./allure-results --clean",
    "run_historico-allure-results": "npx allure serve allure-results",
    "run_bdd_playwright": "npx bddgen && npx playwright test",
    "run_playwright": "npx playwright test",
    "raspadinha": "npx bddgen && npx playwright test --grep @instantaneoRaspadinha",
    "contratacao": "npx bddgen && npx playwright test --grep @contratacao",
    "livelo": "npx bddgen && npx playwright test --grep @livelo",
    "NomeSocial04": "npx bddgen && npx playwright test --grep @NomeSocial_CT004",
    "bsidgetcep": "npx bddgen && npx playwright test --grep @bsidgetcep",
    "validacpf": "npx bddgen && npx playwright test --grep @validacpf",
    "novoerroprivado": "npx bddgen && npx playwright test --grep @novoerroprivado",
    "consultaerroprivado": "npx bddgen && npx playwright test --grep @consultaerroprivado",
    "erroprivado": "npx bddgen && npx playwright test --grep @erroprivado",
    "resumo": "npx bddgen && npx playwright test --grep @bsidresumo",
    "gerarCpf": "npx bddgen && npx playwright test --grep @gerarCpf",
    "bsid": "npx bddgen && npx playwright test --grep @bsid",
    "processachancela": "npx bddgen && npx playwright test --grep @processachancela"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
